import XCTest
@testable import LLMKeyring

final class ExampleTests: XCTestCase {
    func testExampleProviderDefaults() {
        // Placeholder: adjust when Models are visible to tests
        // Ensures we can instantiate basic types without crashing.
        // e.g., let p = Provider(name: "Test", kind: .openAICompatible, baseURL: "https://api.example.com")
        XCTAssertTrue(true)
    }
}

